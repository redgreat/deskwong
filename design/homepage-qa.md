# 首页视觉验证 — 2026-09-12

- 目标：用户第一张图的单色布局与层次，RaceBox 标志来自第三张附件。
- 实现：400×300 LVGL 固件页面，周一首列，五/六行适配；月份/日期/时钟分级，今日反白；天气、农历、宜、AI 额度侧栏；点位无“点”后缀。
- 根因：旧中文字体 bitmap_format=1，但 sdkconfig 中 LV_USE_FONT_COMPRESSED 关闭，中文不可见。所有新字库 bitmap_format=0、bpp=1。
- 预览：`design/final/ui-preview.png`、`ui-preview-six.png`、`ui-preview-empty.png`；实际 main_screen.cpp + LVGL + 固件相同 RGB565 单色阈值。
- 已检查：中文字形、五行与六行月历农历/进度条间距、今日白色进度、顶部状态与数字边界、侧栏分隔和无数据占位。
- 预览断言通过：禁用解压仍可取到“年”字形；界面最多使用 88960/131072 字节。
- ESP-IDF v5.5.1 编译通过：`firmware/build/deskwong.bin`，0x18d6f0 字节，应用分区剩余 61%。此文件为应用镜像（项目 ota_0 地址 0x20000），不是地址 0 的合并镜像。
- 限制：演示工时/天气/额度不代表实时数据；400×300 像素限制下，格内保留简短农历，完整农历在侧栏。城市名称未由现有天气接口返回，未硬编码参考图中的杭州。
- 实机待验证：尚未刷写设备，SPI 刷新与实际反射屏对比度未通过照片验收。
