# 02-日历主屏实施

> 模块：日历主屏 | 状态：实施中 | 最近更新：2026-09-06

## 变更记录
| 日期 | 版本 | 变更内容 | 变更人 |
| --- | --- | --- | --- |
| 2026-09-06 | v1.0 | 初版 | 项目负责人 |
| 2026-09-06 | v1.1 | 新增 AI 用量展示区（占位，M5 接真实数据） | 项目负责人 |
| 2026-09-06 | v1.2 | 月历网格 UI + 中文字体已实现并编译通过；农历/节气/节假日/调休/今天视觉区分就绪 | AI |

---

## 1. 任务拆解

| # | 任务 | 内容 | 产出 | 依赖 |
| --- | --- | --- | --- | --- |
| 2.1 | LVGL 点亮 | 移植官方 LVGL 例程，跑通 400×300 横屏全屏渲染 | 屏显 demo | M0 |
| 2.2 | 农历算法 | 集成农历算法库，公历↔农历互转 | `calendar.c` 基础 | D1 |
| 2.3 | 节假日数据 | 内置年度节假日/调休表 + 解析加载 | 节假日数据模块 | D1 |
| 2.4 | 月历渲染 | 月历网格：公历+农历+周末+节假日+今天 | 主屏月历区 | 2.1~2.3 |
| 2.5 | 天气模块 | 天气 API 客户端 + 缓存 + 图标映射 | 主屏天气区 | D2 |
| 2.6 | 温湿度模块 | SHTC3 驱动 + 定时采集 | 主屏温湿度区 | — |
| 2.7 | 倒计时 | 最近节假日倒计时计算 + 轮播 | 主屏倒计时区 | 2.3 |
| 2.8 | 状态栏 | 时间/网络/电量图标 | 顶部状态栏 | 2.1 |
| 2.9 | 工时占位 | 工时摘要占位（接 M3 数据） | 主屏工时区 | M3 |
| 2.10 | 组装与联调 | 各区排版、局部刷新、降级容错 | 完整主屏 | 2.4~2.9 |
| 2.11 | AI 用量占位 | AI 用量展示区占位（接 M5 数据，`rules/05`） | 主屏 AI 用量区 | M5 |

## 2. 关键技术要点

- **布局**：400×300 横屏。建议：
  - 顶部状态栏（约 24px）
  - 月历主体（约 300×320，7 列 × 6 行，每格显示公历大字 + 农历小字）
  - 底部信息区（约 300×56）：左温湿度、中天气、右倒计时/工时摘要/AI 用量，可多页轮播。
- **局部刷新**：只更新变化格/文本，避免整屏重绘（RLCD 刷新特性，注意残影与刷新策略）。
- **配色**：工作日/周末/节假日/调休上班日/今天 用不同前景/背景色，主题可切换（浅色/深色）。
- **降级**：天气、工时、传感器、AI 用量任一失败显示占位，不阻塞。

## 3. 接口与数据结构

- 农历：`lunar_from_solar(y,m,d)` → 农历月/日/节气/生肖。
- 节假日：`holiday_of(date)` → `{type: 法定节假日|调休上班|周末|普通, name}`。
- 倒计时：`next_holidays(n)` → `[{name, date, days_left}]`。
- AI 用量：`providers()` → `[{id, name, label, value, unit, display, status}]`（见 `rules/05` §2.2）。

## 4. 里程碑归属

- 本模块对应 **M1**；工时摘要部分在 **M3** 接入真实数据、AI 用量在 **M5** 接入（先占位）。

## 5. 风险与备注

- 农历算法库与节假日表需选权威源（D1），数据不准确会连锁影响提醒模块。
- RLCD 刷新需要按官方例程确认刷新/清屏时序，避免残影。

## 6. 验收清单

- [ ] 月历正确（农历/节气/周末/节假日/调休/今天）
- [ ] 天气正确 + 降级
- [ ] 温湿度实时
- [ ] 倒计时正确
- [ ] 状态栏正确
- [ ] 工时占位 → M3 后真实
- [ ] AI 用量占位 → M5 后真实
- [ ] 单项失败不崩溃

## 2026-09-12 首页视觉修订
- 已完成：修复压缩字库与固件配置不匹配，增加未压缩单色字号。
- 已完成：按参考图重排状态栏、月历和侧栏，替换 RaceBox 标志，保留纯数字点位数量。
- 验证通过：主机直接运行 LVGL 页面，覆盖正常、无数据、六行月份；字体回归断言通过，界面堆峰值 88960/131072 字节；ESP-IDF 编译通过，应用 0x18d6f0 字节；实机验收待刷机。

## 2026-09-12 实机显示排查
- 对照五个参考项目（其中 wudingjian 仓库按 README 跟进到实际源码），核对单色字库、400×300 像素映射、SPI 初始化和全屏/局部刷新。
- 新版标识为 0.2.0-ui2；启动日志和 /api/health 提供版本、编译时间和运行分区，防止把旧固件当新版。
- 编译或资源打包失败必须终止刷机；串口完整更新写入 OTA 初始选择数据，确保启动此次写入的 ota_0。
- 优先读取设备原始镜像核对并备份，再验证应用；不得用主机预览代替实机启动证据。
- 实机进一步验证完成：COM7 写入最终 0.2.0-ui2，CRC/哈希校验通过；LVGL 128 KB 池移到 PSRAM；启动后片内剩余 70167 字节；45 秒日志无崩溃重启。物理画面待用户确认。

## 2026-09-12 后台与额度修订
- 排查网页读取 SPIFFS 时 PSRAM 任务栈与 Flash cache 禁用冲突；HTTP 任务栈使用片内 RAM，LVGL 对象池继续使用 PSRAM。
- 实施工时无单位、ChatGPT 图标标题、5小时/每周重置时间、额度仅进度条；为聚合 API 增加窗口、余量及重置时间字段。
- 验证：实机访问网页及登录，正常/无数据/六行月份主机渲染。
- 已完成并实机验证：0.2.1-ui3 HTTP 栈改为片内 RAM，首页及 CSS/JS 10 轮访问、登录和保存配置通过，串口无崩溃；工时单位移除、ChatGPT 图标及重置时间布局完成；额度数据解析回归通过。

## 2026-09-12 天气与服务修订
- 天气请求地址、LocationID/经纬度与刷新分钟数进入配置后台及 NVS。
- 外部 HTTP 拉取移入独立后台任务；WiFi 断开时不请求，避免主屏时钟与交互被网络超时拖慢。
- 天气区显示室外湿度，并增加 SHTC3 室内温湿度行。
- 新增 `service/` Go 服务，提供工时、AI 用量 API，并支持 MQTT JSON 入 PostgreSQL；标签发布同时推送 GHCR、阿里云 ACR 和 GitHub Release。
